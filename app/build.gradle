    buildTypes {
        release {
            minifyEnabled true 
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        debug {
            // Hemos eliminado el bloque de crashlytics que causaba el error
            minifyEnabled false
        }
    }
